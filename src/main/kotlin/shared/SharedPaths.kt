package shared

class SharedPaths
{
    companion object {
        //Insert in host and port accordingly to your dbeaver/VM. The same will apply to database, username and password.
        val JDBC_CONNECTION_STRING = "jdbc:mysql://{host}:{port}/olirum_boys"
        val USERNAME = ""
        val PASSWORD = ""
    }
}